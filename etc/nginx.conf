server {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;

    location ^~ /uploads/ {
          root /home/box/web;
    }

    location ~* ^.+\.\w+$ {
          root /home/box/web/public;
    }

    location /hello/ {
          proxy_pass http://app_servers/hello/;
    }

    location / {
        proxy_pass http://app_servers;
    }

#     location / {
#           return 404;
#     }
}